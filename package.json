{
  "name": "seed-server",
  "description": "Standard server for hosting seeds.  You can start a seed server by using the command 'seed server' or access this package to integrate the service into your own web server",
  "version": "0.2.0",
  
  "dependencies": {
    "seed": "0.1",
    "node-router": "0.1",
    "core-support": "0.1",
    "cradle": "0.1.0"
  },
  
  "seed:commands": {
    "server":  "seed-server:command"
  }
  
}